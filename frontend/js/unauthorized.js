document.addEventListener("DOMContentLoaded", () => {

    setTimeout(() => {

        // Redirecting back to login page in the same frontend
        window.location.href = "login.html";
    }, 3000);

});